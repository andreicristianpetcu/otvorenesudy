<% if subscriptions.any? %>
  <div class="page-header">
    <h2>
      <%= t '.notifikácie' %>
    </h2>
  </div>

  <%= render 'subscriptions/list', model: Hearing, subscriptions: subscriptions, offset: 0 %>
<% end %>
